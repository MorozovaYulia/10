#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    float A, s;
    int N, i;
    cout << "Enter the numbers \n";
    cin >> A >> N;
    s = 1;
    for (i = 1; i <= N; i++) {
        s += pow(A, i);
    }
    cout << s;
}
