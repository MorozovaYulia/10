#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    float A, sum;
    int N, i;
    cout << "Enter the numbers \n";
    cin >> A >> N;
    sum = 1;
    for (i = 1; i <= N; i++) {
        sum += pow(A, i);
    }
    cout << sum;
}
