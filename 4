#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    float A, sum;
    int N, i;
    cout << "Vvedite A, N: " << endl;
    cin >> A >> N;
    sum = 1;
    for (i = 1; i <= N; i++) {
        sum += pow(A, i);
    }
    cout << sum << endl;
}
